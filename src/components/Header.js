import Logo from "./Logo";
import Nav from "./Nav";

function Header() {
    return (
        <header className="Header">
            <Logo />
            <Nav /> 
        </header>
    );
}

export default Header;